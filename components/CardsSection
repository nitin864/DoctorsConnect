import React from "react";
import HeroCard from "./HeroCard";

type CardItem = {
  variant?: "blue" | "purple" | "coral" | "green";
  eyebrow?: string;
  title?: React.ReactNode;
  description?: string;
  cta?: { text: string; href?: string };
  imageSrc?: string;
};

const cards: CardItem[] = [
  {
    variant: "blue",
    eyebrow: "FOR PROVIDERS",
    title: (
      <>
        Empower your <span className="font-bold">practice</span> with iHealth AI
      </>
    ),
    description:
      "Streamline operations and improve patient outcomes with integrated appointment, EMR, and AI tools.",
    cta: { text: "Learn More", href: "/providers" },
    imageSrc: "/demo/provider-demo.png",
  },
  {
    variant: "purple",
    eyebrow: "FOR PATIENTS",
    title: "Discover a healthier you",
    description:
      "Track your vitals, manage prescriptions, and get personalised care plans powered by AI.",
    cta: { text: "Learn More", href: "/patients" },
    imageSrc: "/demo/patient-demo.png",
  },
  {
    variant: "coral",
    eyebrow: "FOR PARTNERS",
    title: "Develop a healthcare solution using Eka Care",
    description:
      "Build integrations, extend workflows and deliver a customised experience for your users.",
    cta: { text: "Explore APIs", href: "/partners" },
    imageSrc: "/demo/developer-demo.png",
  },
];

export default function CardsSection() {
  return (
    <section className="max-w-4xl mx-auto px-6 py-12 flex flex-col gap-6">
      {cards.map((card, idx) => (
        <div key={idx} className="transition-transform hover:-translate-y-1">
          <HeroCard
            variant={card.variant}
            eyebrow={card.eyebrow}
            title={card.title}
            description={card.description}
            cta={card.cta}
            imageSrc={card.imageSrc}
          />
        </div>
      ))}
    </section>
  );
}
